export interface StockData {
    symbol: string;
    companyName: string;
    name: string;
    close: string;
    change: string;
    percentChange: number;
    volume: string;
    previousClose: string;
}